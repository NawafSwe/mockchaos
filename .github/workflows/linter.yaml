name: "Linter"

on:
  pull_request:
    branches: [ master,main ]
    paths-ignore:
      - "README.md"
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: golangci-lint
        run: make lint